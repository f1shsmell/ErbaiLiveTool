<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Erbai.App.Views.DiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:diagnostics="using:Erbai.App.Diagnostics"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls">

    <Page.Resources>
        <diagnostics:LevelToBrushConverter x:Key="LevelToBrush" />
        <diagnostics:LevelToTextConverter x:Key="LevelToText" />
    </Page.Resources>

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="诊断" Style="{StaticResource PageTitleTextStyle}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,4,0,0">
            <Button x:Name="RefreshButton" Content="刷新" Click="OnRefresh" />
            <Button x:Name="ExportButton" Content="导出脱敏报告" Click="OnExport" />
            <TextBlock x:Name="ExportStatus" VerticalAlignment="Center" Opacity="0.8" />
        </StackPanel>

        <ListView Grid.Row="2" x:Name="DiagnosticsList" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="diagnostics:DiagnosticItem">
                    <Grid ColumnSpacing="12" Padding="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64" />
                            <ColumnDefinition Width="240" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border CornerRadius="8" Padding="8,2" VerticalAlignment="Center"
                                Background="{x:Bind Level, Converter={StaticResource LevelToBrush}}">
                            <TextBlock Text="{x:Bind Level, Converter={StaticResource LevelToText}}"
                                       FontSize="12" Foreground="White" />
                        </Border>
                        <TextBlock Grid.Column="1" Text="{x:Bind Name}" FontWeight="SemiBold"
                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Detail}" TextWrapping="Wrap"
                                   VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
