<Project Sdk="Microsoft.NET.Sdk">
  <!-- 连接器宿主 exe（子进程，决策 #15）：NDJSON-stdio 服务端循环 + lxmusic 原生后端。
       网易云 / 酷狗 / QQ 音乐 / Folia 已插件化（用户按清单安装上游第三方连接器 exe），
       不再由本工程实现，也不再随包分发其 bridge/profiles 资产。
       机制与数值参考旧实现（04 §1.3-1.4），代码表达沿用上游。 -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Erbai.Contracts\Erbai.Contracts.csproj" />
  </ItemGroup>
</Project>
